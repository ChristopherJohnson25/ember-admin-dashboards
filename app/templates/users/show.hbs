{{outlet}}
<br />
<br />

<div class="container">
    <div class="row">
    <div class="col-xs-12 col-sm-12 col-md-12">
      <div class="well well-sm">
        <div class="row">
          <div class="col-sm-6 col-md-4">
            {{#if user.avatar}}
            <img src={{user.avatar}} alt="" class="img-rounded img-responsive" />
            {{else}}
            <img src="http://placehold.it/380x500" alt="" class="img-rounded img-responsive" />
            {{/if}}
          </div>
          <div class="col-sm-6 col-md-8">
            {{file-upload user=user store=store name=user.name session=session.currentUser.displayName}}
            <h3>{{user.name}}</h3>
            <span class="label label-primary">Primary</span>
            <br />
            <small><cite title="San Francisco, USA">{{user.city}} <i class="glyphicon glyphicon-map-marker">
            </i></cite></small>
            <p>
              <i class="glyphicon glyphicon-envelope"></i> <a href="mailto:{{user.email}}">{{user.email}}</a>
              <br />
              <i class="glyphicon glyphicon-phone"></i> <a href="tel:{{user.phone}}">{{user.phone}}</a>
              <br />
              <!-- <i class="glyphicon glyphicon-globe"></i><a href="http://www.jquery2dotnet.com">www.jquery2dotnet.com</a> -->
              <i class="glyphicon glyphicon-gift"></i>{{user.DOB}}</p>
            <!-- Split button -->
            <!-- <div class="btn-group">
              <button type="button" class="btn btn-primary">
                Social</button>
              <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown">
                <span class="caret"></span><span class="sr-only">Social</span>
              </button>
              <ul class="dropdown-menu" role="menu">
                <li><a href="#">Twitter</a></li>
                <li><a href="https://plus.google.com/+Jquery2dotnet/posts">Google +</a></li>
                <li><a href="https://www.facebook.com/jquery2dotnet">Facebook</a></li>
                <li class="divider"></li>
                <li><a href="#">Github</a></li>
              </ul>
            </div> -->
          </div>
        </div>
      </div>
    </div>
  </div>
  {{user-notes user=user store=store}}
  <!-- {{user-actions user=user store=store}} -->
  <div class="well well-sm">
    <table class="table table-striped">
      <thead>
        <tr>
          <th scope="col">#</th>
          <th scope="col">Link</th>
          <th scope="col">Type</th>
          <th scope="col">Uploaded By</th>
        </tr>
      </thead>
      <tbody>
        {{#each user.documents as |documents i|}}
          <tr>
            <th scope="row">{{plus-one i}}</th>
            <td><a target="_blank" href="{{documents.url}}">{{documents.name}}</a></td>
            <td>{{documents.type}}</td>
            <td>{{documents.uploadedBy}}</td>
            <td>{{user.phone}}</td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  </div>

</div>
